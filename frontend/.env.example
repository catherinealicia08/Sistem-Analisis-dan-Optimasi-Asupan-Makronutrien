# Local development uses the Vite /api proxy by default.
# Production should point directly to the deployed backend URL.
# Example: https://macroplus-backend.vercel.app
VITE_API_URL=https://YOUR_BACKEND_URL.vercel.app
